# Git 설정 기록

작성일: 2026-04-09

## 수행 내용

- 현재 디렉토리에서 Git 저장소를 새로 초기화했습니다.
- 기본 브랜치를 `main`으로 설정했습니다.
- Python 프로젝트 기준의 기본 `.gitignore` 파일을 추가했습니다.

## 변경 파일

- `.gitignore`

## 다음 단계

아래 명령으로 첫 커밋을 진행할 수 있습니다.

```bash
git add .
git commit -m "Initial commit"
```
